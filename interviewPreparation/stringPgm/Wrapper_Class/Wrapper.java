package Wrapper_Class;
import java.util.*;
public class Wrapper {
public static void main(String[] args) {
	/*
	 * //Converting int into Integer String a="20"; Integer
	 * i=S.valueOf(a);//converting int into Integer explicitly //Integer i = new
	 * Integer(a); // depricate rarly used so. Integer j=a;//autoboxing, now
	 * compiler will write Integer.valueOf(a) internally
	 * 
	 * System.out.println(a+" "+i+" "+j);
	 */
}
}
